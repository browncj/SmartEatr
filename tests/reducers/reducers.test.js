import expect from 'expect';
import smartEatrApp from '../../reducers/reducers';

describe('smartEatrApp (combined reducers)', () => {
  it('should exist', () => {
    expect(smartEatrApp).toExist();
  });
});
